# Public browser configuration only. Do not put API keys, JWT secrets,
# database URLs, SMS credentials, OTP values or service-role keys here.
VITE_AGENT_NAME=sahayak-ai
VITE_API_BASE_URL=http://127.0.0.1:8000
